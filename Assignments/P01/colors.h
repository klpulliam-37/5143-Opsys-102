#include <string>

namespace colors {
    std::string RED();
    std::string GREEN();
    std::string BLUE();
    std::string LIGHT_BLUE();
    std::string YELLOW();
    std::string RESET();
    std::string RemoveAnsi(std::string input);
}